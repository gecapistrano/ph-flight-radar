import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PH Flight Radar — live traffic over the Philippine FIR",
  description:
    "Live aircraft positions over the Philippine Flight Information Region, drawn from the OpenSky Network's open ADS-B feed.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="h-full overflow-hidden bg-slate-950 antialiased" suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}
